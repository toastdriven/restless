language: python

matrix:
  include:
    - python: "2.7"
    - python: "3.5"
    - python: "3.6"
    - python: "3.7"
      dist: xenial
    - python: "pypy" # CPython 2.7

env:
  - DJANGO="1.8"
  - DJANGO="1.9"
  - DJANGO="1.10"
  - DJANGO="1.11"

# command to install dependencies
install:
    - pip install six tox-travis

# command to run tests
script:
    - tox
